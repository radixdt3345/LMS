# T-008 — AUTH-E2E-001: E2E Login Flows

## Type
E2E

## Issue Reference
Issue ID: AUTH-E2E-001 | Feature: F-01 | Epic: E-01

## Test IDs Owned
| Test ID | Type | Description |
|---------|------|-------------|
| E2E-1 | E2E @smoke | Local login → Employee Dashboard |
| E2E-2 | E2E | SSO login → Dashboard |

## Implementation Steps
1. Create `e2e/auth.spec.ts`
2. E2E-1 (@smoke): `await page.goto('/login')`; fill email + password; click 'Sign In'; `await expect(page).toHaveURL('/dashboard')`; assert user name in header
3. E2E-2: mock MSAL redirect via `page.route()` to return mock ID token; assert dashboard
4. Tag E2E-1 with `@smoke` in test title
5. Environment: staging (baseURL from env `PLAYWRIGHT_BASE_URL`)
6. Trigger: workflow_dispatch ONLY (never on PR push)

## Dependencies
Blocked by: T-005, T-006
Unblocks: T-015

## Definition of Done
- [ ] E2E-1 tagged @smoke; completes in < 60s
- [ ] Both tests pass on staging environment
- [ ] workflow_dispatch trigger only (no PR trigger)
- [ ] PR merged
