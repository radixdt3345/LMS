# T-010 — AUTH-UI-002: Locked Accounts Management Page

## Type
Development

## Issue Reference
Issue ID: AUTH-UI-002 | Feature: F-02 | Epic: E-02

## Technical Specification

### Files to Create/Modify
| File path | Action | Description |
|-----------|--------|-------------|
| `frontend/src/pages/admin/LockedAccountsPage.tsx` | Create | HR Admin locked accounts page |
| `frontend/src/api/adminApi.ts` | Create | Axios calls for admin endpoints |

## Implementation Steps
1. Create `LockedAccountsPage` at route `/admin/users/locked`; wrap in `RoleProtectedRoute allowedRoles={['HRAdmin','SuperAdmin']}`
2. MUI DataGrid: columns — name, email, locked_since (lockout_until), failed_attempts, Actions
3. Unlock button per row: calls POST /admin/users/{id}/unlock; on success remove row (optimistic update); on error show MUI Snackbar error
4. Empty state: "No locked accounts" when list is empty
5. Add link in sidebar nav (HR Admin + Super Admin only)

## Test IDs Owned
| Test ID | Type | Description |
|---------|------|-------------|
| E2E-3 | E2E | HR Admin unlocks account via UI |

## Dependencies
Blocked by: T-009
Unblocks: T-011

## Definition of Done
- [ ] Page inaccessible to Employee/Manager (RoleProtectedRoute enforces)
- [ ] Unlock removes row; empty state shown correctly
- [ ] /lsp: zero TypeScript Errors | PR merged
