# T-041 — LEAVECORE-DB-004: leave_requests + approval_steps Tables

## Type
Development

## Issue Reference
Issue ID: LEAVECORE-DB-004 | Feature: F-09 | Epic: E-09

## Database Changes
| Table | Operation | Fields |
|-------|-----------|--------|
| leave_requests | CREATE | id uuid PK, employee_id uuid FK→users NOT NULL, leave_type_id uuid FK→leave_types NOT NULL, start_date date NOT NULL, end_date date NOT NULL, computed_days numeric(5,1) NOT NULL DEFAULT 0, status smallint NOT NULL DEFAULT 0 (0=Draft,1=Pending,2=Approved,3=Rejected,4=Cancelled,5=Revoked), is_retroactive bool NOT NULL DEFAULT false, reason text NOT NULL, document_url text NULL, created_at timestamptz, updated_at timestamptz |
| approval_steps | CREATE | id uuid PK, leave_request_id uuid FK→leave_requests CASCADE DELETE, step_number smallint NOT NULL, approver_id uuid FK→users NOT NULL, status smallint NOT NULL DEFAULT 0 (0=Pending,1=Approved,2=Rejected), acted_at timestamptz NULL, comment text NULL, created_at timestamptz |

## Indexes
- leave_requests: INDEX(employee_id), INDEX(status), INDEX(start_date), COMPOSITE INDEX(employee_id, status)
- approval_steps: INDEX(leave_request_id), INDEX(approver_id, status)

## Implementation Steps
1. Create `LeaveRequest.cs`, `ApprovalStep.cs` entities; `LeaveRequestStatus.cs`, `ApprovalStepStatus.cs` enums
2. Configure FK constraints; approval_steps CASCADE DELETE on leave_request delete
3. EF migration `CreateLeaveRequestsAndApprovalSteps`

## Dependencies
Blocked by: T-031, T-021
Unblocks: T-042

## Definition of Done
- [ ] Migration clean | All indexes created | Cascade delete verified | /lsp zero Errors | PR merged
