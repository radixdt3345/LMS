# T-031 — LEAVECORE-DB-002: leave_balances Table + Hangfire Schema

## Type
Development

## Issue Reference
Issue ID: LEAVECORE-DB-002 | Feature: F-07 | Epic: E-07

## Database Changes
| Table | Operation | Fields |
|-------|-----------|--------|
| leave_balances | CREATE | id uuid PK, user_id uuid FK→users NOT NULL, leave_type_id uuid FK→leave_types NOT NULL, year smallint NOT NULL, allocated_days numeric(5,1) NOT NULL DEFAULT 0, used_days numeric(5,1) NOT NULL DEFAULT 0, carried_forward_days numeric(5,1) NOT NULL DEFAULT 0, created_at timestamptz, updated_at timestamptz; UNIQUE(user_id, leave_type_id, year) |

## Implementation Steps
1. Create `LeaveBalance.cs` entity
2. Add UNIQUE constraint on (user_id, leave_type_id, year)
3. Create EF migration `CreateLeaveBalances`
4. In same migration or separate: ensure Hangfire schema initialized — add `services.AddHangfire(c => c.UsePostgreSqlStorage(connStr))` in Program.cs

## Dependencies
Blocked by: T-026, T-021
Unblocks: T-032, T-041

## Definition of Done
- [ ] Migration clean | UNIQUE (user_id, leave_type_id, year) enforced | Hangfire tables in hangfire schema | /lsp zero Errors | PR merged
