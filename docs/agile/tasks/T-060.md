# T-060 — NOTIFICATIONS-UI-001: Notification Bell and Polling

## Type
Development

## Issue Reference
Issue ID: NOTIFICATIONS-UI-001 | Feature: F-12 | Epic: E-12

## Files
- `frontend/src/components/NotificationBell.tsx` | Create
- `frontend/src/store/notifications/notificationsSlice.ts` | Create

## Implementation Steps
1. NotificationBell in AppBar: MUI IconButton with Badge; badge count from Redux notificationsSlice
2. Polling: useEffect with setInterval (60s) calling GET /notifications/unread-count; dispatch update to Redux
3. Dropdown: MUI Popover; list of last 10 notifications; each item: title + timestamp + read/unread state
4. Click notification: POST /notifications/{id}/read; navigate to resource (leave request detail)
5. "Mark all read" button: POST /notifications/read-all; clear badge

## Test IDs Owned
E2E-11

## Dependencies
Blocked by: T-059
Unblocks: T-061

## Definition of Done
- [ ] 60s polling interval (not WebSocket) | Badge updates | /lsp zero TypeScript Errors | PR merged
