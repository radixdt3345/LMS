# T-036 — LEAVECORE-DB-003: Create holidays Table

## Type
Development

## Issue Reference
Issue ID: LEAVECORE-DB-003 | Feature: F-08 | Epic: E-08

## Database Changes
| Table | Operation | Fields |
|-------|-----------|--------|
| holidays | CREATE | id uuid PK, date date NOT NULL UNIQUE, name varchar(200) NOT NULL, is_optional bool NOT NULL DEFAULT false, created_at timestamptz NOT NULL |

## Implementation Steps
1. Create `Holiday.cs` entity; date stored as `DateOnly`
2. Add DbSet; create `HolidayConfiguration.cs`: UNIQUE(date), INDEX(date)
3. EF migration `CreateHolidays`

## Dependencies
Blocked by: T-013
Unblocks: T-037

## Definition of Done
- [ ] Migration clean | UNIQUE(date) enforced | /lsp zero Errors | PR merged
