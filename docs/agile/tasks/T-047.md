# T-047 — COMPOFF-DB-001: comp_off_requests + comp_off_credits Tables

## Type
Development

## Issue Reference
Issue ID: COMPOFF-DB-001 | Feature: F-10 | Epic: E-10

## Database Changes
| Table | Operation | Fields |
|-------|-----------|--------|
| comp_off_requests | CREATE | id uuid PK, employee_id uuid FK→users, worked_date date NOT NULL, worked_hours numeric(4,1) NOT NULL, status smallint NOT NULL DEFAULT 0 (0=Pending,1=Approved,2=Rejected), created_at timestamptz, updated_at timestamptz; UNIQUE(employee_id, worked_date) |
| comp_off_credits | CREATE | id uuid PK, employee_id uuid FK→users, comp_off_request_id uuid FK→comp_off_requests, credit_days numeric(3,1) NOT NULL, expires_at date NOT NULL, used_days numeric(3,1) NOT NULL DEFAULT 0, created_at timestamptz |

## Implementation Steps
1. Create entities + enums
2. UNIQUE(employee_id, worked_date) on comp_off_requests
3. expires_at computed in service: worked_date + 180 days
4. EF migration `CreateCompOffTables`

## Dependencies
Blocked by: T-021
Unblocks: T-048

## Definition of Done
- [ ] Migration clean | UNIQUE(employee_id, worked_date) | /lsp zero Errors | PR merged
