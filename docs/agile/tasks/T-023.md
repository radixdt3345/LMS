# T-023 — PEOPLE-UI-002: Employee Management and Profile Pages

## Type
Development

## Issue Reference
Issue ID: PEOPLE-UI-002 | Feature: F-05 | Epic: E-05

## Files to Create/Modify
| File path | Action | Description |
|-----------|--------|-------------|
| `frontend/src/pages/admin/EmployeesPage.tsx` | Create | HR Admin employee management |
| `frontend/src/pages/profile/ProfilePage.tsx` | Create | Employee own profile |
| `frontend/src/api/employeesApi.ts` | Create | Axios CRUD calls |

## Implementation Steps
1. `/admin/employees` (HRAdmin+): DataGrid — employee_code, name, department, manager, role (auto-derived badge), is_active, Actions; Add/Edit dialog with all fields; Deactivate button with confirmation
2. Manager dropdown: only shows users with role=Manager; department dropdown from cached list
3. Role badge auto-updates: if manager assigned → show "Manager" (derived); if no direct reports → show base role
4. `/profile` (all): show own fields; edit form for first_name, last_name, phone only; role shown as read-only badge
5. `/employees/team` (Manager): DataGrid of direct reports; name, email, leave balance summary

## Dependencies
Blocked by: T-022
Unblocks: T-024

## Definition of Done
- [ ] CRUD works end-to-end | Role badge shows correct derived value | /lsp zero TypeScript Errors | PR merged
