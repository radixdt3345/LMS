# T-058 — NOTIFICATIONS-DB-001: Create notifications Table

## Type
Development

## Issue Reference
Issue ID: NOTIFICATIONS-DB-001 | Feature: F-12 | Epic: E-12

## Database Changes
| Table | Operation | Fields |
|-------|-----------|--------|
| notifications | CREATE | id uuid PK, user_id uuid FK→users NOT NULL, type smallint NOT NULL, title varchar(200) NOT NULL, body text NOT NULL, is_read bool DEFAULT false, resource_type varchar(50) NULL, resource_id uuid NULL, created_at timestamptz NOT NULL |

## Indexes
- COMPOSITE INDEX(user_id, is_read, created_at DESC) — for unread count query
- INDEX(user_id, created_at DESC) — for notification list

## Implementation Steps
1. Create `Notification.cs` entity; `NotificationType.cs` enum
2. EF migration `CreateNotifications`; apply both indexes

## Dependencies
Blocked by: T-021
Unblocks: T-059

## Definition of Done
- [ ] Migration clean | Composite index on (user_id, is_read, created_at) | /lsp zero Errors | PR merged
