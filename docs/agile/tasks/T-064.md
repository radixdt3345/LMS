# T-064 — REPORTING-DB-001: Create audit_logs Table

## Type
Development

## Issue Reference
Issue ID: REPORTING-DB-001 | Feature: F-14 | Epic: E-14

## Database Changes
| Table | Operation | Fields |
|-------|-----------|--------|
| audit_logs | CREATE | id uuid PK, action varchar(100) NOT NULL, entity_type varchar(100) NOT NULL, entity_id uuid NOT NULL, actor_id uuid FK→users NOT NULL, old_value jsonb NULL, new_value jsonb NULL, created_at timestamptz NOT NULL |

## CRITICAL: This table is APPEND-ONLY.
- No UPDATE migration ever
- No DELETE endpoint ever
- No EF update/delete configuration for this entity

## Indexes
- COMPOSITE INDEX(entity_type, entity_id)
- INDEX(actor_id)
- INDEX(created_at DESC)

## Implementation Steps
1. Create `AuditLog.cs` entity with NO navigation for update/delete
2. Configure in EF: `modelBuilder.Entity<AuditLog>().HasNoKey()` is wrong — use uuid PK but NO update/delete in DbContext operations
3. EF migration `CreateAuditLogs`

## Dependencies
Blocked by: T-021
Unblocks: T-065

## Definition of Done
- [ ] Migration clean | Indexes created | /lsp zero Errors | PR merged
