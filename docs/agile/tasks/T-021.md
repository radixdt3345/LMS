# T-021 — PEOPLE-DB-002: Users Profile Columns + Manager Self-Ref FK

## Type
Development

## Issue Reference
Issue ID: PEOPLE-DB-002 | Feature: F-05 | Epic: E-05

## Database Changes
| Table | Operation | Fields |
|-------|-----------|--------|
| users | ALTER | ADD first_name varchar(100) NOT NULL, last_name varchar(100) NOT NULL, phone varchar(20) NULL, join_date date NOT NULL DEFAULT CURRENT_DATE, manager_id uuid NULL REFERENCES users(id), department_id uuid NOT NULL REFERENCES departments(id), employee_code varchar(50) UNIQUE NOT NULL |

## Implementation Steps
1. Modify `User.cs` entity: add all profile properties
2. Add self-referencing navigation: `User? Manager`, `ICollection<User> DirectReports`
3. Update `UserConfiguration.cs`: manager_id nullable FK self-ref; department_id FK RESTRICT on delete; UNIQUE(employee_code); indexes on department_id + manager_id
4. Add EF migration: `AddUserProfileColumns`

## Test IDs Owned
| Test ID | Type | Description |
|---------|------|-------------|
| IT-11 | Integration | Create employee; verify all columns in DB |

## Dependencies
Blocked by: T-016
Unblocks: T-022, T-031, T-041, T-047, T-053, T-058, T-064

## Definition of Done
- [ ] Migration runs cleanly | employee_code UNIQUE verified | manager_id nullable | /lsp zero Errors | PR merged
