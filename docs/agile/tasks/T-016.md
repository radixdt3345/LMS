# T-016 — PEOPLE-DB-001: Create departments Table

## Type
Development

## Issue Reference
Issue ID: PEOPLE-DB-001 | Feature: F-04 | Epic: E-04

## Database Changes
| Table | Operation | Fields |
|-------|-----------|--------|
| departments | CREATE | id uuid PK, name varchar(100) NOT NULL UNIQUE, description text NULL, is_active bool DEFAULT true, created_at timestamptz NOT NULL, updated_at timestamptz NOT NULL |

## Implementation Steps
1. Create `LMS.Domain/Entities/Department.cs`
2. Add `DbSet<Department>` to LmsDbContext
3. Create `DepartmentConfiguration.cs`: snake_case, UNIQUE(name), soft-delete pattern (is_active filter in queries)
4. Add EF migration: `dotnet ef migrations add CreateDepartments -p LMS.Infrastructure -s LMS.API`

## Test IDs Owned
| Test ID | Type | Description |
|---------|------|-------------|
| IT-9 | Integration | departments table exists; CRUD works against real DB |

## Dependencies
Blocked by: T-013 (seed creates first dept)
Unblocks: T-017, T-021

## Definition of Done
- [ ] Migration runs cleanly | name UNIQUE constraint verified | /lsp zero Errors | PR merged
