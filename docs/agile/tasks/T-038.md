# T-038 — LEAVECORE-UI-003: Holiday Calendar Page (FullCalendar)

## Type
Development

## Issue Reference
Issue ID: LEAVECORE-UI-003 | Feature: F-08 | Epic: E-08

## Files
- `frontend/src/pages/HolidaysPage.tsx` | Create
- `frontend/src/api/holidaysApi.ts` | Create

## Implementation Steps
1. Route `/holidays` (all authenticated): FullCalendar month view; holidays rendered as red non-clickable events
2. Year selector: fetches /holidays?year=; re-renders calendar
3. HR Admin controls: "+ Add Holiday" dialog (date picker, name, is_optional toggle); Delete icon on each holiday event
4. CSV Import button: drag-drop or file picker; POST /holidays/bulk-import; show import summary dialog with errors per row
5. Employee/Manager: read-only (no add/delete controls)

## Test IDs Owned
| Test ID | Type | Description |
|---------|------|-------------|
| E2E-10 | E2E | HR Admin uploads CSV; calendar shows new holidays |

## Dependencies
Blocked by: T-037
Unblocks: T-039

## Definition of Done
- [ ] FullCalendar shows holidays | CSV import shows row-level errors | /lsp zero TypeScript Errors | PR merged
