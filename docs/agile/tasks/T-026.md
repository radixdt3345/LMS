# T-026 — LEAVECORE-DB-001: Create leave_types Table

## Type
Development

## Issue Reference
Issue ID: LEAVECORE-DB-001 | Feature: F-06 | Epic: E-06

## Database Changes
| Table | Operation | Fields |
|-------|-----------|--------|
| leave_types | CREATE | id uuid PK, name varchar(100) NOT NULL UNIQUE, max_days_per_year integer NULL, accrual_type smallint NOT NULL (0=Annual,1=OneTime,2=Unlimited — no monthly/quarterly per org policy), requires_document bool NOT NULL DEFAULT false, is_active bool DEFAULT true, created_at timestamptz NOT NULL, updated_at timestamptz NOT NULL |

## Implementation Steps
1. Create `LeaveType.cs` entity + `AccrualType.cs` enum
2. Add `DbSet<LeaveType>` to LmsDbContext
3. Create `LeaveTypeConfiguration.cs`: UNIQUE(name), max_days_per_year nullable
4. Add EF migration `CreateLeaveTypes`

## Dependencies
Blocked by: T-013
Unblocks: T-027, T-031

## Definition of Done
- [ ] Migration clean | UNIQUE(name) enforced | max_days_per_year nullable | /lsp zero Errors | PR merged
